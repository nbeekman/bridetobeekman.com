const styles = {
  container: {
    height: '100%',
    width: '100%',
  },
  contentContainer: {
    height: `${window.innerHeight - 97}px`,
    width: '100%',
    display: 'inline-block',
    position: 'absolute',
    right: '0px',
  },
};

export default styles;
